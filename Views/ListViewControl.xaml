<UserControl x:Class="Project.Views.ListViewControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="clr-namespace:Project.ViewModels"
             Height="Auto" Width="Auto">
    <Grid Background="#F9FAFB">
        <StackPanel>
            <TextBlock Text="{Binding Title}" FontWeight="Bold" FontSize="20" Margin="0,0,0,10"/>

            <DataGrid ItemsSource="{Binding Items}" AutoGenerateColumns="False" CanUserAddRows="False" RowHeight="30">
                <DataGrid.Columns>
                    <!-- Dynamically bind columns depending on model -->
                    <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="50"/>
                    <DataGridTextColumn Header="Name/Email/Software" Binding="{Binding DisplayName}" Width="*"/>
                    <DataGridTextColumn Header="Extra Info" Binding="{Binding Extra}" Width="150"/>
                    <DataGridTextColumn Header="Renewal / Registered" Binding="{Binding DateInfo, StringFormat=dd MMM yyyy}" Width="120"/>
                    <DataGridTextColumn Header="Amount / Storage" Binding="{Binding AmountOrStorage}" Width="100"/>

                    <DataGridTemplateColumn Header="Actions" Width="150">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                    <Button Content="Edit" Command="{Binding DataContext.EditCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" CommandParameter="{Binding}" Margin="5,0"/>
                                    <Button Content="Delete" Command="{Binding DataContext.DeleteCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" CommandParameter="{Binding}" Background="#EF4444" Foreground="White" Margin="5,0"/>
                                </StackPanel>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>

                </DataGrid.Columns>
            </DataGrid>
        </StackPanel>
    </Grid>
</UserControl>
